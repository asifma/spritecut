# spritecut
Browser-based audio sprite editor — drop an MP3, auto-detect sounds, name and trim segments, export Howler.js JSON. No install, single HTML file. README explains the tool, Howler.js format, features, waveform controls table, iterative workflow, detection settings, export format, and browser compatibility.
